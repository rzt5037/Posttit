New comment to "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= @post.user.name %>

This post will now be sent email notifications for new comments.

Visit online at: <%= topic_post_url(@post.topic, @post) %>
